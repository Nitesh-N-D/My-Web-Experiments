# Student REST API (Node.js)

A simple RESTful API built using Node.js core modules (`http`, `fs`).

## Endpoints
- GET / → Health check
- GET /students
- GET /students/:id
- POST /students
- PUT /students/:id
- DELETE /students/:id

## Run
node server.js
